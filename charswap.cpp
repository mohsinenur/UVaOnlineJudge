#include<bits/stdc++.h>
using namespace std;
int main()
{
    char a[1];
    char b[1];
    cin >> a[0];
    cin >> b[0];

    swap(a[0],b[0]);

    cout << a[0] << " " << b[0] << endl;

    return 0;
}
